<h2><slot /></h2>

<style lang="scss">
	h2 {
		font-size: var(--font-60);
		font-weight: var(--font-weight-light);
		line-height: 1.2;
		font-family: var(--font-sans);
		color: var(--clr-txt);
	}
</style>
